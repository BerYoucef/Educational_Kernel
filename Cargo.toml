[package]
name = "Educational_Kernel"
version = "0.1.0"
edition = "2024"


[dependencies]
bootloader = "0.9"

# used on cargo build
[profile.dev]
panic = "abort" # disable stack unwinding on panic

# used on cargo build --realse 
[profile.release]
panic = "abort" # disable stack unwinding on panic